Balises html à revoir // sémentique SEO

Largest Contentful Paint (LCP) : taille de transfert des
images trop importante

Format des images : préférer un format "nouvelle génération"

Dimension des images : images responsives à privilégier (définir largeur et hauteur)

Ressources qui bloquent l'affichage : bonne utilisation des script, link
utiliser seulement l'essentiel por l'ouverture de la page
(onglet couverture de devTools)

minimiser le css et JS

modifier la duréée de vie du cache pour les ressources statiques

réduire les ressources JS inutilisées

Décalage cumlatif de mise en page (CLS) : changement de mise en page trop important

